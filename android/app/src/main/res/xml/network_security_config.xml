<?xml version="1.0" encoding="utf-8"?>
<!--
  Network security policy for iHO-now Sales Tracker.

  Cleartext (HTTP) traffic is only permitted to the local development hosts
  used during `flutter run` (real device via ADB reverse `127.0.0.1`, and the
  Android emulator `10.0.2.2`). All other hosts — including the production API
  at iho.intigroup.top — must use HTTPS. This keeps release builds from ever
  sending credentials over plaintext while still allowing local dev.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
